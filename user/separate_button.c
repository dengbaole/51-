#include "separate_button.h"
#include "delay.h"

///按下KEY1led按位左移
unsigned char key_state(int i)
{
    if(i==1)
    {
        if(KEY1==0)
        {
            while(KEY1==0)
            {
                ;
            }
            
        }
    }
    return ;
}